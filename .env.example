# Production (Netlify): MUST be your Railway API base URL — NOT the Netlify site URL.
# Example: https://soundwave-api-production.up.railway.app
# No trailing slash.
VITE_API_URL=

# Local dev: leave empty to use Vite proxy to localhost:4000
